#include<stdio.h>

int main()
{
int M.N;

while[0] {
printf("%d", M, N);
scanf("%d\n", &M, &N);
    prinf("entrada segunda"\n);
    while(get char () 1 - \n);

}

if(m - 0, N - 0)
       break

return (0);
}
